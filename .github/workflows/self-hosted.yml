name: Test Self-hosted Runner

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v3
      - name: Say Hello from EC2
        run: echo "Hello from self-hosted EC2 runner!"
